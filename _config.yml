# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve [--baseurl '']'. If you change this file, please restart the server process.

# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
title: Andrea Silva | Design
email: esausilva85@gmail.com
description: > 
    A starter project including full setup for Jekyll, GulpJS, SASS, PostCSS, AutoPrefixer & BrowserSync.
    Ready for deployment to GitHub pages with gulp-gh-pages. 
baseurl: "/andreasilva" # the subpath of your site, e.g. /blog
url: "http://yourusername.github.io/andreasilva" # the base hostname & protocol for your site, e.g. http://example.com

# Build settings
markdown: kramdown
exclude: ["node_modules", "gulpfile.js", "package.json"]
gems:
    - jekyll-minifier
jekyll-minifier:
  exclude: "*.css"
